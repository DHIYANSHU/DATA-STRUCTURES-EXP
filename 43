#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50, 60, 70};
    int size = sizeof(arr) / sizeof(arr[0]);

    if (size >= 5) {
        printf("The 5th iterated element (at index 4) is: %d\n", arr[4]);
    } else {
        printf("Array does not have a 5th element.\n");
    }
    return 0;
}
